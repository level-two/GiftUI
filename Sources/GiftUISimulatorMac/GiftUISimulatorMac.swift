import AppKit
import CoreGraphics
import GiftUI
import GiftUIBackendFramebuffer

/// Namespace for the AppKit-only simulator shell.
public enum GiftUISimulatorMacModule {
    public static let name = GiftUIModule.name + "SimulatorMac"
}
