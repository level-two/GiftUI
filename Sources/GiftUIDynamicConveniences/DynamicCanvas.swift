import GiftUI

/// An opt-in Dynamic-profile spelling of the portable `Canvas` declaration.
///
/// This is an exact type alias: it adds no semantic case, runtime query, or
/// alternate drawing behavior.
public typealias DynamicCanvas = Canvas
