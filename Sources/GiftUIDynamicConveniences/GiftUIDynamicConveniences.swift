import GiftUI

/// Namespace for opt-in APIs that permit unbounded storage or escaping
/// callbacks.
public enum GiftUIDynamicConveniencesModule {
    public static let name = GiftUIModule.name + "DynamicConveniences"
}
