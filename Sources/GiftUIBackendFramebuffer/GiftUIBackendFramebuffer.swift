import GiftUI

/// Namespace for the platform-neutral in-memory framebuffer backend.
public enum GiftUIBackendFramebufferModule {
    public static let name = GiftUIModule.name + "BackendFramebuffer"
}
