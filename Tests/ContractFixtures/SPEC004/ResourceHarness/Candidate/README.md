# SPEC-004 Resource Candidate

The matched build generates one whole-module Swift input from the exact
checked-in production capability source plus the shared observable probe. It
executes construction, resolution, result/snapshot storage, and retained
access without substituting Spike code.
