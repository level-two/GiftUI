# SPEC-006 T7.2 Deferred-Work Review

FW-017 and FW-020 remain `captured`, unpromoted, and outside every milestone.
Both retain SPEC-006 provenance, reciprocal Specification and plan links,
explicit non-goals, and concrete Proposal/evidence/application triggers. The
2026-09-09 revisit found that none of those triggers has fired, so the recorded
disposition is to continue each item as optional post-MVP work.

The executable audit rejects either ID from maintained GiftUI/Semantic Core
source and from every non-evidence SPEC-006 fixture input. It also permits the
IDs only in the plan's original authority audit and this deferred-work review,
not in implementation tasks. No new optional discovery arose during the review,
and current scope, acceptance criteria, implementation, and profile evidence
remain unchanged.
