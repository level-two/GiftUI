# T2.4 Semantic Recording and Borrow Lifetimes

The canonical transcript records stable structural identities, concrete action
tokens, normalized codes, stored-label child paths, modifier order, semantic
order, and effective enabled state. The Button poison-counter test proves the
source builder runs only at initialization; SPEC-006 atomic traversal tests
prove payload/action/label borrows do not survive expansion. The normalizer
copies only `UInt16`; no declaration, payload, label, handler, or model is
stored in candidate-facing state.
