# SPEC-011 T8.2 Complete Interaction Corpus

Every profile driver runs the registered declaration, candidate, gesture,
dispatch, failure, and target-generation audits plus the complete Interaction,
Interaction failure-adapter, Runtime interaction, and profile-differential test
suites. The normalized fixtures compare symbolic identity, action code,
action/target generations, enabled state, checked geometry, paint order,
candidate transition, gesture result, dispatch result, failure, and
publication state; private addresses and profile storage bytes are excluded.

The four immutable 2026-09-19 driver modes completed with one shared input
identity. Their composed SPEC-013 profile reports provide the zero-tolerance
Dynamic/Static transcript comparison, and their focused corpus covers exact
limits, first excess, initial binding, preservation, replacement, discard,
stale capture, movement cancellation, disabled overlap, model replacement,
failure precedence, mandatory containment, and diagnostic isolation.

This is hardware-free host execution plus cross-build evidence. It makes no
connected pointer, display, or board claim.
